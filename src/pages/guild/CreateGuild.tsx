import ComingSoonPage from "@/components/layout/ComingSoonPage";

const CreateGuild = () => {
  return <ComingSoonPage title="Create Guild" description="Start a new guild and invite members." />;
};

export default CreateGuild;
